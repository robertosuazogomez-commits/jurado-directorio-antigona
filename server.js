const express=require('express');const path=require('path');const app=express();const PORT=process.env.PORT||10000;const SUPABASE_URL=process.env.SUPABASE_URL;const SUPABASE_KEY=process.env.SUPABASE_ANON_KEY;app.use(express.json({limit:'20kb'}));app.use(express.static(path.join(__dirname,'public')));app.get('/api/health',(_req,res)=>res.json({ok:true,supabaseConfigured:!!(SUPABASE_URL&&SUPABASE_KEY)}));app.post('/api/vote',async(req,res)=>{if(!SUPABASE_URL||!SUPABASE_KEY)return res.status(500).json({ok:false,error:'Supabase no está configurado en Render.'});const {presentacion,estudiante,bando,argumentacion,refutacion,actuacion,justificacion,evidencia}=req.body||{};const p=Number(presentacion);const scores=[argumentacion,refutacion,actuacion].map(Number);if(!Number.isInteger(p)||p<1||p>20)return res.status(400).json({ok:false,error:'Presentación inválida.'});if(typeof estudiante!=='string'||estudiante.trim().length<2||estudiante.trim().length>100)return res.status(400).json({ok:false,error:'Nombre inválido.'});if(!['A','B'].includes(bando))return res.status(400).json({ok:false,error:'Bando inválido.'});if(scores.some(n=>!Number.isInteger(n)||n<1||n>5))return res.status(400).json({ok:false,error:'Puntuación inválida.'});if(typeof justificacion!=='string'||justificacion.trim().length<5||justificacion.trim().length>2000)return res.status(400).json({ok:false,error:'Justificación inválida.'});if(typeof evidencia!=='string'||evidencia.trim().length<5||evidencia.trim().length>2000)return res.status(400).json({ok:false,error:'Evidencia inválida.'});try{const r=await fetch(`${SUPABASE_URL}/rest/v1/votaciones`,{method:'POST',headers:{apikey:SUPABASE_KEY,Authorization:`Bearer ${SUPABASE_KEY}`,'Content-Type':'application/json',Prefer:'return=minimal'},body:JSON.stringify({presentacion:p,estudiante:estudiante.trim(),bando,argumentacion:scores[0],refutacion:scores[1],actuacion:scores[2],justificacion:justificacion.trim(),evidencia:evidencia.trim()})});if(!r.ok){console.error('Supabase:',r.status,await r.text());return res.status(502).json({ok:false,error:'No se pudo guardar el veredicto en Supabase.'})}res.json({ok:true,message:'Veredicto registrado correctamente.'})}catch(err){console.error(err);res.status(502).json({ok:false,error:'Error de conexión con Supabase.'})}});app.get('*',(_req,res)=>res.sendFile(path.join(__dirname,'public','index.html')));app.listen(PORT,'0.0.0.0',()=>console.log(`Jurado del Directorio funcionando en puerto ${PORT}`));